# Progress log

- 2026-09-20T00:00:00 | orchestrator | Phase 0: pyproject/requirements pinned (Python 3.11, not 3.12 -- not installed on this machine, noted as a deviation), models.py/db.py/mirror.py/providers base+claude written per sections 6 and 10, tests/test_db.py green, four subagent files created, five synthetic transcripts generated covering acme-platform + north-star, person Jordan Alvarez, one finance mention (rent), one health mention (sleep/caffeine), and the team/auth decision that changes between transcript 2 and transcript 4. | next: run librarian, retriever, qa in parallel (Phase 1).
- 2026-09-20T13:15:00 | librarian | Built src/vault/ingest/{markdown,claude_code,chatgpt}.py (all returning list[ParsedConversation] from a shared dataclass in ingest/__init__.py: text/chat_id/started_at), scrub.py (regex + Luhn + Shannon-entropy redaction for all eight categories in section 9, ordered so specific patterns run before the generic high-entropy catch-all), extract.py (scrubs then calls provider.extract with EXTRACTION_SCHEMA, validates against ExtractionResult, retries once with the ValidationError appended, raises on second failure), supersede.py (apply_extraction: upserts entities, same-value bumps confidence, different-value inserts+supersedes, no-prior just inserts, writes the episode). Added tests/test_ingest.py, tests/test_scrub.py, tests/test_extract.py, tests/test_supersede.py -- 29 tests, all green with a fake Provider (no real API calls); ruff clean on all owned paths. Ran a manual end-to-end check (markdown ingest -> extract -> supersede -> mirror) against a real synthetic transcript to confirm the pipeline composes. Judgment calls: extract()'s fixed signature returns only ExtractionResult, so redaction counts for CLI display ("secrets redacted") aren't threaded through it -- cli.py should call scrub.scrub() directly (it's idempotent) if it needs the counts for printing; ExtractedEntity has no category field but entities table requires one, so supersede.py assigns an entity the category of the first fact that references it, falling back to a kind-based guess (people/projects) for entities mentioned only in extraction.entities with no fact. | next: integrator wires ingest/extract/supersede into cli.py's `vault ingest` command.
- 2026-09-20T13:00:00 | retriever | Built src/vault/retrieve/{gate,scope,search,pack}.py per PRD section 8: gate.needs_memory (possessive/trigger-phrase/fuzzy-entity/scope rules, Levenshtein within one edit), scope.resolve_scope (explicit > prompt entity > chat pin > none) and scope.allow_sensitive (finance/health keyword lists), search.search (FTS5 bm25 over facts+episodes, scope as a hard SQL filter, current-facts-only, newer-observed_at tie-break, sensitive facts dropped unless allow_sensitive=True), pack.pack (exact block shape from 7.1, profile/facts/episodes order, skips db.logged_item_ids, truncates an episode before dropping a fact, fixed-point token counting via models.count_tokens). Added tests/test_gate.py (10 yes/10 no), tests/test_scope.py, tests/test_search.py, tests/test_pack.py -- 47 tests, all green; ruff clean on src/vault/retrieve. Judgment calls (no db.py/mirror.py/PRD contract change needed, but noting for integrator): search() returns one merged list of dicts tagged kind="fact"/"episode" (caller splits by kind before calling pack); pack()'s fixed signature has no scope arg, so it infers the header's scope label from facts[0]["entity"] (or "none"); a fact only gets a "(was X, date)" suffix when db history shows a superseded predecessor, otherwise just "predicate = value" (PRD's illustrative ", decided <date>" variant wasn't algorithmically specified, so I didn't try to reverse-engineer which facts get which suffix). | next: integrator wires gate/scope/search/pack into cli.py and mcp_server.py.
- 2026-09-20T12:49:22-04:00 | qa | Built eval/golden.jsonl (25 rows: 8 gate=no, 6 acme-platform-scoped, 5 north-star-scoped, 2 person/Jordan, 2 finance, 2 health, including the auth sessions->Clerk decision-change row), src/vault/eval.py (run_eval: gate accuracy, recall@5/precision@5 against expected_entity_ids resolved by name via db.get_entity_by_name since raw autoincrement ids are not stable across LLM-driven extraction runs, token mean/p95 via models.count_tokens, writes eval/results.md, appends PROGRESS.md; degrades to a reported not-available state instead of raising if src/vault/retrieve is not importable), tests/test_no_leaks.py (inserts facts/episodes for all 5 synthetic transcripts directly via db.py with short paraphrased evidence, runs every mirror.py writer, asserts no 12-word span from inbox/synthetic/ appears in memory/ or PROGRESS.md), tests/test_eval.py. Verified end-to-end against the now-landed src/vault/retrieve/*.py and src/vault/{scrub,extract,supersede}.py: gate accuracy 1.0, recall@5 0.69, precision@5 0.34, tokens mean 120/p95 163 on a hand-built dataset -- confirms the contracts compose. Fixed one real bug while doing so: eval.py builds its own FTS5 query from the prompt (stopword-filtered, OR-joined) since search.search()'s bareword MATCH is an implicit AND and a full raw sentence matches nothing. pyproject.toml/requirements.txt dependency list already matches PRD section 5 exactly, no changes needed. 95 tests green repo-wide, ruff check . clean repo-wide. | next: integrator wires run_eval into vault eval in cli.py; orchestrator runs the real vault ingest and vault eval in Phase 3 for the numbers that go in eval/results.md.
